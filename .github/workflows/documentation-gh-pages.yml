name: documentation

on: [push, pull_request, workflow_dispatch]

permissions:
  contents: write

jobs:
  pages:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    permissions:
      pages: write
      id-token: write
    steps:
      - id: deployment
      - uses: sphinx-notes/pages@v3
      - name: Install dependencies
        run: |
          pip install sphinx sphinx_rtd_theme myst_parser m2r
          pip install -e .
      - name: Sphinx build
        run: |
          sphinx-build docs _build
